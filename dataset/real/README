Note:
1. The data here is only for the training data. They will be used to feature extraction and then clustering to mine the identities in target domain.
2. Just in the origial filename, but NOT the filename generated by the transform_xxx.py.
